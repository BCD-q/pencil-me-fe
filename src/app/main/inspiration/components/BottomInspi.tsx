import BottomCarousel from './BottonComponent';

export default function BottonInspiration() {
  return (
    <div className="carousel carousel-vertical h-full bg-white">
      <BottomCarousel />
      <BottomCarousel />
      <BottomCarousel />
      <BottomCarousel />
      <BottomCarousel />
    </div>
  );
}
